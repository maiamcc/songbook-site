---
layout: base.njk
---
{% import "macros.njk" as m with context %}
<p class="print-tip">For a printer-friendly version of this song, visit the <a href="print/">print view</a>.</p>
<article class="song">
  <header class="song-header">
    <p class="layout-toggle"><a href="print/">Print view →</a></p>
    <h1>
      {{ title }}{% if alternate_title %} <span class="alt-title">({{ alternate_title }})</span>{% endif %}
    </h1>
    {# Under the title: a two-column flex row. The left column stacks
       the byline (author / year) and the collapsible Metadata drawer;
       the right column is .song-vitals (Bop and rnge, stacked). The
       row only renders if at least one of its children would. #}
    {% if author or genre or mood or structure or topics or bop_rating or rnge %}
    <div class="meta-row">
      <div class="meta-left">
        {% if author %}
        <p class="byline">{{ author }}</p>
        {% endif %}
        {% if genre or mood or structure or joiny_inny or topics %}
        <details class="song-meta-drawer">
          <summary>Metadata</summary>
          <dl class="song-meta">
            {% if genre %}<dt>Genre</dt><dd>{{ m.indexLink("genre", genre, genre) }}</dd>{% endif %}
            {% if mood %}<dt>Mood</dt><dd>{{ m.indexLink("mood", mood, mood) }}</dd>{% endif %}
            {% if structure %}<dt>Structure</dt><dd>{{ m.indexLink("structure", structure, structure) }}</dd>{% endif %}
            {% if joiny_inny %}<dt>Joiny-inny</dt><dd>{{ m.enumLink("joiny_inny", joiny_inny) }}<span class="enum-legend" title="{{ enums.joiny_inny | enumHelpText }}" aria-label="Joiny-inny help">?</span></dd>{% endif %}
            {% if topics %}<dt>Topics</dt><dd>{% for t in topics %}{{ m.indexLink("topics", t, t) }}{% if not loop.last %}, {% endif %}{% endfor %}</dd>{% endif %}
          </dl>
        </details>
        {% endif %}
      </div>
      {% if bop_rating or rnge %}
      <div class="song-vitals">
        {% if bop_rating %}
        <dl class="song-meta">
          <dt>Bop</dt><dd class="rating">{{ m.indexLink("bop_rating", bop_rating, bop_rating ~ " / 5") }}</dd>
        </dl>
        {% endif %}
        {% if rnge %}
        {% set rngeParts = rnge.split("-") %}
        <dl class="song-meta">
          <dt class="visually-hidden">Range</dt><dd><span class="range">{{ rngeParts[0] }}<svg class="range-arrow" viewBox="0 0 10 10" aria-hidden="true"><path d="M 1.5 5 H 8.5 M 6 3 L 8.5 5 L 6 7" stroke="currentColor" stroke-width="0.6" stroke-linecap="round" stroke-linejoin="round" fill="none" /></svg>{{ rngeParts[1] }}</span></dd>
        </dl>
        {% endif %}
      </div>
      {% endif %}
    </div>
    {% endif %}

  </header>

  <div class="song-body">
    {{ content | safe }}
  </div>

  {% if notes %}
  <hr class="notes-sep" />
  <p class="notes"><span class="notes-label">Notes:</span> {{ notes | inlineMarkdown | safe }}</p>
  {% endif %}
</article>

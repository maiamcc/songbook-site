{
  "name": "songbook-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
